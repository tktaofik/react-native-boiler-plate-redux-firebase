import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  rightHeaderButton: {
    width: 40,
    height: 40,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 5,
  },
});
